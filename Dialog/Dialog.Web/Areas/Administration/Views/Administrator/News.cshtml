@model AdministrationEntityViewModel<NewsSummaryViewModel, AuthorsWithNewsCountViewModel>

<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-10">
            <h1 class="page-header">News</h1>
        </div>
        <!-- /.col-lg-8 -->
        <div class="col-lg-2">
            <h1 class="page-header"><a asp-area="News" asp-controller="News" asp-action="Create" class="btn btn-primary">Create News</a></h1>
        </div>
        <!-- /.col-lg-4 -->
    </div>
    <!-- /.row -->
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    Blog Posts
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead>
                            <tr>
                                <th>Title</th>
                                <th>Author</th>
                                <th>Created On</th>
                                <th>Modified On</th>
                                <th class="col-sm-3">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var news in Model.Entities)
                            {
                                <tr class="d-flex">
                                    <td>@news.Title</td>
                                    <td>@news.Author</td>
                                    <td>@news.CreatedOn.ToString("dd.MM.yyyy")</td>
                                    <td class="center">@news.ModifiedDateText</td>
                                    <td class="col-sm-3">
                                        <div>

                                            <a asp-area="News" asp-controller="News" asp-action="Details" asp-route-id="@news.Id" class="btn btn-success">View</a>

                                            <a asp-area="News" asp-controller="News" asp-action="Edit" asp-route-id="@news.Id" class="btn btn-success">Edit</a>
                                            <a asp-area="News" asp-controller="News" asp-action="Delete" asp-route-id="@news.Id" class="btn btn-danger">Delete</a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <!-- /.table-responsive -->
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
    <div class="row">
        <div class="col-lg-6">
            <div class="panel panel-default">
                <div class="panel-heading">
                    News By Users
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Author</th>
                                    <th>Posts Count</th>
                                </tr>
                            </thead>
                            <tbody>
                                @{ int index = 1;}
                                @foreach (var author in Model.Authors)
                                {
                                    <tr>
                                        <td>@(index++)</td>
                                        <td>@author.UserName</td>
                                        <td>@author.News</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <!-- /.table-responsive -->
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-6 -->
        <!-- /.col-lg-6 -->
    </div>
    <!-- /.row -->
</div>